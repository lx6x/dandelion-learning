spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.80.100:5432/q
    username: postgres
    password: postgres
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
