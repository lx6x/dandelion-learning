server:
  port: 30042
#spring:
#  application:
#    name: dandelion-actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    env:
      show-values: always
    health:
      show-details: always

